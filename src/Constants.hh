#ifndef FLAGIT_CONSTANTS_HH
#define FLAGIT_CONSTANTS_HH

#include <string>

namespace flagit {
    std::string ENABLED = "enabled";
    std::string ENABLED_FOR = "enabled_for";
    std::string DISABLED_FOR = "disabled_for";
} // close namespace flagit

#endif //FLAGIT_CONSTANTS_HH
